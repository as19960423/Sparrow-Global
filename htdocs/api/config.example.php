<?php

/**
 * Конфигурация API Global Sparrow.
 *
 * 1. Скопируйте этот файл в config.php (рядом, в той же папке api/).
 * 2. Заполните значения ниже.
 *
 * config.php добавлен в .gitignore и закрыт от внешнего доступа через .htaccess —
 * секреты не попадут ни в git, ни наружу.
 */

return [
    // Токен бота. Получить: написать @BotFather в Telegram -> /newbot
    'telegram_bot_token' => '',

    // ID чата, куда слать заявки (личка, группа или канал).
    // Узнать: напишите боту /start, затем откройте
    // https://api.telegram.org/bot<ТОКЕН>/getUpdates и возьмите chat.id
    // (у групп ID отрицательный).
    'telegram_chat_id' => '',

    // --- Google Sheets через Google Forms (опционально) ---
    // Создайте форму с короткими текстовыми вопросами и привяжите её к таблице
    // («Ответы» -> значок Google Sheets). Подробная инструкция — в README.md.
    // Оставьте google_form_url пустым, чтобы отключить запись в таблицу.

    // Ссылка на форму (подойдёт обычная ссылка «Отправить» .../viewform)
    'google_form_url' => '',

    // Соответствие полей заявки entry-ID вопросов формы.
    // Узнать ID: откройте форму, ПКМ -> «Просмотреть код страницы», найдите "entry."
    // (или соберите prefill-ссылку: три точки -> «Создать заранее заполненную ссылку»).
    'google_form_fields' => [
        'name'      => 'entry.0000000',
        'whatsapp'  => 'entry.0000000',
        'goal'      => 'entry.0000000',
        'score'     => 'entry.0000000',
        'discount'  => 'entry.0000000',
        'promoCode' => 'entry.0000000',
        'timestamp' => 'entry.0000000',
    ],
];
